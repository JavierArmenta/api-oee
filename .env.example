# ===========================================
# LINEALYTICS API - PLANTILLA DE CONFIGURACIÓN
# ===========================================
# Copia este archivo como .env y completa los valores

ASPNETCORE_ENVIRONMENT=Development

# --- Cadena de Conexión PostgreSQL (misma base de datos que webapp_claude) ---
CONNECTION_STRING=Host=localhost;Port=5432;Database=webapp;Username=webapp_user;Password=

# --- CORS (orígenes permitidos, separados por coma) ---
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173

# --- Swagger/OpenAPI ---
ENABLE_SWAGGER=true
